<article class="w-full flex flex-col pb-5 justify-center items-center md:flex-row mt-14 bg-zinc-100" id="home">
  <slot />
</article>

<style>
  article {
    background: radial-gradient(circle, #ffa68f 10%, transparent 11%);
        background-size: 1em 1em;
  }
  @media (min-width: 768px) {
    article {
      background-image: linear-gradient(to right, #ff5851 50%, transparent 50%);
      background-size: auto;
      height: calc(100vh - 3.5rem);
      max-height: 900px;
    }
  }
</style>
