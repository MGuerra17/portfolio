---
const { dark, fitToScreen } = Astro.props;
---

<article
  class={`w-full flex flex-col md:flex-row flex-wrap py-10 min-h-max ${
    dark ? "bg-neutral-900" : "bg-zinc-100"
  } relative items-center justify-center`}
>
  <slot />
</article>
